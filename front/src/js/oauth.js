function fetchExt(path)
{
    // const nuevaVentana = window.open(base + '/api' + path, "_blank", "width=600,height=400");

    /* if (!nuevaVentana) {
        alert("¡Habilita las ventanas emergentes para esta página!");
    } */
}
